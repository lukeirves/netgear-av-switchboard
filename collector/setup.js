const form=document.querySelector('#settings'),message=document.querySelector('#message'),source=document.querySelector('#sourceAddress'),bind=document.querySelector('#bindAddress'),rescan=document.querySelector('#rescan');let current={};
function option(item){const o=document.createElement('option');o.value=item.address;o.textContent=`${item.name}  ·  ${item.address}  (${item.cidr})`;return o}
async function loadInterfaces(){rescan.disabled=true;rescan.textContent='Discovering interfaces…';try{const r=await fetch('/api/interfaces'),data=await r.json();source.replaceChildren(new Option('Detect automatically',''));bind.replaceChildren(new Option('All available interfaces','0.0.0.0'));for(const item of data.interfaces){source.append(option(item));bind.append(option(item))}source.value=current.sourceAddress||'';bind.value=current.bindAddress||'0.0.0.0';if(!source.value&&current.sourceAddress){const custom=new Option(`Saved interface · ${current.sourceAddress}`,current.sourceAddress);source.append(custom);source.value=current.sourceAddress}if(!bind.value&&current.bindAddress){const custom=new Option(`Saved interface · ${current.bindAddress}`,current.bindAddress);bind.append(custom);bind.value=current.bindAddress}rescan.textContent=`↻ ${data.interfaces.length} interface${data.interfaces.length===1?'':'s'} found`}catch{rescan.textContent='Could not discover interfaces'}finally{rescan.disabled=false}}
async function load(){const r=await fetch('/api/config');current=await r.json();for(const key of ['subnet','username','pollSeconds'])form.elements[key].value=current[key]??'';if(current.hasAuthKey){form.elements.authKey.placeholder='Saved — leave blank to keep';document.querySelector('#authHint').textContent='A key is securely retained locally.'}if(current.hasPrivKey){form.elements.privKey.placeholder='Saved — leave blank to keep';document.querySelector('#privHint').textContent='An AES key is securely retained locally.'}await loadInterfaces()}
rescan.addEventListener('click',loadInterfaces);form.addEventListener('submit',async e=>{e.preventDefault();message.className='busy';message.textContent='Saving settings and starting discovery…';const data=Object.fromEntries(new FormData(form));try{const r=await fetch('/api/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),result=await r.json();if(!r.ok)throw new Error(result.error);message.className='success';message.textContent=result.restartRequired?'Settings saved. Restart Switchboard to apply the client interface change.':'Settings saved. Scanning the switch network now…';form.elements.authKey.value='';form.elements.privKey.value='';setTimeout(()=>location.href='http://localhost:3000',1800)}catch(error){message.className='error';message.textContent=error.message}});load().catch(()=>{message.className='error';message.textContent='Could not load server settings. Start Switchboard and try again.'});
